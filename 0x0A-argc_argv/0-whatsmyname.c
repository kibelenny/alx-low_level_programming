#include <stdio.h>

/**
 * main - prints its name
 * @argc: no. command lines
 * @argv: array containing command lines arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
